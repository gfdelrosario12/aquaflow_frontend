/// Severity categorization for system notifications
enum AlertSeverity { info, warning, critical }

/// High-level domain category of the event
enum AlertCategory { agronomic, hardware, irrigation, connectivity }

/// Physical origin type of the alert event
enum AlertSourceType { centralIrrigation, monitoringZone, gateway }

