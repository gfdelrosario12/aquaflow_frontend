/// Operational health status classification for hardware devices
enum DeviceHealthStatus { healthy, degraded, offline, stale, error }

/// Physical hardware device category
enum DeviceCategory { sensorNode, gateway, centralController }
