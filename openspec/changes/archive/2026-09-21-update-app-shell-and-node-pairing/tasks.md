## 1. App Shell and Navigation Updates

- [x] 1.1 Update `lib/features/shell/presentation/app_shell.dart` to remove Analytics tab and set up 5 navigation items.
- [x] 1.2 Verify `HomeScreen` navigation routing to tab index 1 (Field) when zone/quadrant quick actions are tapped.

## 2. Settings Hardware Node Pairing Integration

- [x] 2.1 Add hardware node pairing & discovery section and pairing dialog trigger to `lib/features/settings/presentation/settings_screen.dart`.

## 3. Verification & Testing

- [x] 3.1 Update navigation and settings widget tests for 5-tab app shell and hardware node pairing UI.
- [x] 3.2 Run full `flutter test` test suite to verify zero regressions.
